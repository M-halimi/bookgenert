# ============================================
# BUILD-TIME PLACEHOLDER — safe to commit
# ============================================
# Prisma reads this file for `prisma generate` (no DB needed).
# Vercel env vars override these values in production.
# For local dev, .env.local overrides these.
# For DB operations, use .env.local or set env vars explicitly.
DATABASE_URL=postgresql://placeholder:placeholder@localhost:5432/placeholder
DIRECT_URL=postgresql://placeholder:placeholder@localhost:5432/placeholder
SHADOW_DATABASE_URL=postgresql://placeholder:placeholder@localhost:5432/placeholder
